export enum RCode {
    success = 200,
    created = 201,
    accepted = 202,
    badRequest = 400,
    unAuthorized = 401,
    forbidden = 403,
    notFound = 404,
    conflict = 409
}

export enum Language {
    vi = "vi",
    en = "en"
}

export enum MessageType {
    sms = "sms",
    email = "email"
}
